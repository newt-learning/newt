import React from "react";
import { StyleSheet, Text } from "react-native";

const BookScreen = () => {
  return <Text style={styles.text}>Book screen</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 48
  }
});

export default BookScreen;
